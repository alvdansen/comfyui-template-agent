---
phase: 12-repo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - pyproject.toml
  - .gitignore
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03

must_haves:
  truths:
    - "MIT LICENSE file exists at repo root with Alvdansen Labs as copyright holder"
    - "pyproject.toml declares version 3.0.0 with author, license, repository URL, classifiers, and keywords"
    - ".gitignore covers .venv/, .planning/, COMPAT-FIX.md, and all existing patterns"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "pyproject.toml"
      provides: "Complete package metadata"
      contains: 'version = "3.0.0"'
    - path: ".gitignore"
      provides: "Comprehensive exclusion patterns"
      contains: ".planning/"
  key_links:
    - from: "pyproject.toml"
      to: "LICENSE"
      via: "license field referencing MIT"
      pattern: 'license.*MIT'
---

<objective>
Create the legal and structural metadata files for a public-ready Python repository.

Purpose: Phase 12 establishes the repo foundation -- license, package metadata, and gitignore -- so downstream phases (README, CI, publishing) can reference correct version, author, and license information.
Output: MIT LICENSE file, updated pyproject.toml with full metadata, updated .gitignore with comprehensive patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-repo-foundation/12-CONTEXT.md
@pyproject.toml
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <read_first>
    - pyproject.toml (to see current project name/description for consistency)
    - .planning/phases/12-repo-foundation/12-CONTEXT.md (D-01 locked decision: Alvdansen Labs as copyright holder)
  </read_first>
  <action>
Create file `LICENSE` at repo root with the standard MIT License text.

Copyright line must read exactly:
```
Copyright (c) 2026 Alvdansen Labs
```

Per D-01: Alvdansen Labs is the sole author and copyright holder. Do NOT use "Timothy" or any personal name -- use "Alvdansen Labs" only.

Use the current year 2026. The full MIT License text is the standard OSI-approved template from opensource.org/licenses/MIT.
  </action>
  <acceptance_criteria>
    - LICENSE file exists at repo root (not in a subdirectory)
    - File contains exactly "MIT License" on the first line
    - File contains "Copyright (c) 2026 Alvdansen Labs"
    - File contains "Permission is hereby granted, free of charge"
    - File contains 'THE SOFTWARE IS PROVIDED "AS IS"'
    - File does NOT contain "Timothy" or "timothy@" or any personal name
  </acceptance_criteria>
  <verify>
    <automated>grep -c "MIT License" LICENSE && grep -c "Copyright (c) 2026 Alvdansen Labs" LICENSE && grep -c "Permission is hereby granted" LICENSE && ! grep -q "Timothy" LICENSE && echo "LICENSE OK"</automated>
  </verify>
  <done>MIT LICENSE file exists at repo root with Alvdansen Labs as copyright holder, standard MIT text, year 2026</done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml with complete metadata</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state -- version 0.1.0, missing metadata fields)
    - .planning/phases/12-repo-foundation/12-CONTEXT.md (D-01, D-02, D-03 locked decisions)
  </read_first>
  <action>
Update `pyproject.toml` to add complete package metadata. Preserve all existing content (dependencies, tool configs) and ADD the following fields to the `[project]` section:

1. Change `version = "0.1.0"` to `version = "3.0.0"`

2. Add these fields after `description`:
```toml
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Alvdansen Labs", email = "timothy@comfy.org"},
]
keywords = ["comfyui", "templates", "workflows", "claude-code", "agent", "ai"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
```

3. Add a `[project.urls]` section after `[project.optional-dependencies]`:
```toml
[project.urls]
Homepage = "https://github.com/alvdansen/comfyui-template-agent"
Repository = "https://github.com/alvdansen/comfyui-template-agent"
Issues = "https://github.com/alvdansen/comfyui-template-agent/issues"
```

Per D-01: Author name is "Alvdansen Labs" (not personal name).
Per D-02: Author email is "timothy@comfy.org".
Per D-03: Repository URL is "github.com/alvdansen/comfyui-template-agent".

Do NOT remove or modify: `requires-python`, `dependencies`, `[project.optional-dependencies]`, `[tool.setuptools.packages.find]`, `[tool.pytest.ini_options]`, or `[tool.ruff]` sections.
  </action>
  <acceptance_criteria>
    - pyproject.toml contains `version = "3.0.0"`
    - pyproject.toml contains `license = {text = "MIT"}`
    - pyproject.toml contains `name = "Alvdansen Labs"`
    - pyproject.toml contains `email = "timothy@comfy.org"`
    - pyproject.toml contains `"comfyui"` in keywords
    - pyproject.toml contains `License :: OSI Approved :: MIT License` in classifiers
    - pyproject.toml contains `Repository = "https://github.com/alvdansen/comfyui-template-agent"`
    - pyproject.toml still contains `requires-python = ">=3.12"`
    - pyproject.toml still contains `httpx` in dependencies
    - pyproject.toml still contains `[tool.ruff]` section
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'version = "3.0.0"' pyproject.toml && grep -c 'license = {text = "MIT"}' pyproject.toml && grep -c 'Alvdansen Labs' pyproject.toml && grep -c 'timothy@comfy.org' pyproject.toml && grep -c 'alvdansen/comfyui-template-agent' pyproject.toml && grep -c 'requires-python' pyproject.toml && grep -c 'tool.ruff' pyproject.toml && echo "PYPROJECT OK"</automated>
  </verify>
  <done>pyproject.toml contains version 3.0.0, Alvdansen Labs author with timothy@comfy.org email, MIT license, alvdansen repo URLs, classifiers, keywords, and all existing config preserved</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore with comprehensive patterns</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state -- 10 patterns, needs additions)
    - .planning/phases/12-repo-foundation/12-CONTEXT.md (D-06, D-07, D-08, D-09 locked decisions)
  </read_first>
  <action>
Replace `.gitignore` with a comprehensive file that includes ALL existing patterns plus new additions. The complete file must contain:

```gitignore
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Testing
.pytest_cache/

# Linting
.ruff_cache/

# Environment
.env

# Cache
data/cache/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# Internal planning (excluded from public repo)
.planning/

# Internal docs (not a deliverable)
COMPAT-FIX.md

# Claude Code local settings
.claude/settings.local.json
```

Per D-06: .venv/ and .planning/ MUST be present.
Per D-07: COMPAT-FIX.md MUST be present (internal bash compatibility guide).
Per D-08: templates/*/build.py are NOT listed -- they are core deliverables and must remain tracked.
Per D-09: All existing patterns (__pycache__/, *.pyc, .pytest_cache/, data/cache/, .claude/settings.local.json, .env, *.egg-info/, dist/, build/, .ruff_cache/) carried forward.
Discretion items: Added .idea/, .vscode/, *.swp, *.swo, *~, .DS_Store, *.pyo for common Python/IDE/macOS artifacts.
  </action>
  <acceptance_criteria>
    - .gitignore contains ".venv/"
    - .gitignore contains ".planning/"
    - .gitignore contains "COMPAT-FIX.md"
    - .gitignore contains "__pycache__/"
    - .gitignore contains "*.pyc"
    - .gitignore contains ".pytest_cache/"
    - .gitignore contains "data/cache/"
    - .gitignore contains ".claude/settings.local.json"
    - .gitignore contains ".env"
    - .gitignore contains "*.egg-info/"
    - .gitignore contains "dist/"
    - .gitignore contains "build/"
    - .gitignore contains ".ruff_cache/"
    - .gitignore contains ".DS_Store"
    - .gitignore does NOT contain "build.py" or "templates/" as an exclusion pattern
  </acceptance_criteria>
  <verify>
    <automated>grep -c ".venv/" .gitignore && grep -c ".planning/" .gitignore && grep -c "COMPAT-FIX.md" .gitignore && grep -c "__pycache__/" .gitignore && grep -c ".DS_Store" .gitignore && ! grep -q "build.py" .gitignore && ! grep -q "templates/" .gitignore && echo "GITIGNORE OK"</automated>
  </verify>
  <done>.gitignore contains all required patterns (.venv/, .planning/, COMPAT-FIX.md, existing 10 patterns, IDE/macOS artifacts) and does NOT exclude templates/*/build.py</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo | LICENSE and metadata become public; no secrets should be in these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | .gitignore | mitigate | Ensure .planning/, .env, and .claude/settings.local.json are excluded so internal artifacts and secrets never reach public repo |
| T-12-02 | Information Disclosure | pyproject.toml | accept | Author email (timothy@comfy.org) is intentionally public contact info, not a secret |
</threat_model>

<verification>
All three files exist and pass their automated verify commands. No tracked files match the new .gitignore patterns (verified by git status showing no unexpected removals).
</verification>

<success_criteria>
- MIT LICENSE file at repo root with "Alvdansen Labs" copyright
- pyproject.toml version 3.0.0 with author, license, URLs, classifiers, keywords
- .gitignore covers .venv/, .planning/, COMPAT-FIX.md, and all existing patterns
- `pytest` still passes (no functional changes)
</success_criteria>

<output>
After completion, create `.planning/phases/12-repo-foundation/12-01-SUMMARY.md`
</output>
