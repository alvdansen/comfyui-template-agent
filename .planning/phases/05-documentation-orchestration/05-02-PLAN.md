---
phase: 05-documentation-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/skills/comfy-flow/SKILL.md
  - src/document/orchestrator.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [ORCH-01, ORCH-02]

must_haves:
  truths:
    - "User can run a guided session that walks through discover, ideate, compose, validate, and document phases"
    - "Each phase provides context-aware suggestions based on previous phase output"
  artifacts:
    - path: ".claude/skills/comfy-flow/SKILL.md"
      provides: "Orchestrator skill definition for guided end-to-end template creation flow"
      contains: "comfy-flow"
    - path: "src/document/orchestrator.py"
      provides: "Session state model and phase transition logic for the guided flow"
      exports: ["FlowSession", "FlowPhase", "advance_phase", "suggest_next_actions"]
    - path: "tests/test_orchestrator.py"
      provides: "Tests for session state, phase transitions, context-aware suggestions"
  key_links:
    - from: ".claude/skills/comfy-flow/SKILL.md"
      to: ".claude/skills/comfy-discover/SKILL.md"
      via: "References discover skill for Phase 1 of the flow"
      pattern: "comfy-discover"
    - from: ".claude/skills/comfy-flow/SKILL.md"
      to: ".claude/skills/comfy-templates/SKILL.md"
      via: "References templates skill for Phase 2 ideation"
      pattern: "comfy-templates"
    - from: ".claude/skills/comfy-flow/SKILL.md"
      to: ".claude/skills/comfy-compose/SKILL.md"
      via: "References compose skill for Phase 3 composition"
      pattern: "comfy-compose"
    - from: ".claude/skills/comfy-flow/SKILL.md"
      to: ".claude/skills/comfy-validate/SKILL.md"
      via: "References validate skill for Phase 4 validation"
      pattern: "comfy-validate"
    - from: ".claude/skills/comfy-flow/SKILL.md"
      to: ".claude/skills/comfy-document/SKILL.md"
      via: "References document skill for Phase 5 documentation"
      pattern: "comfy-document"
    - from: "src/document/orchestrator.py"
      to: "src/document/metadata.py"
      via: "suggest_next_actions uses generate_index_entry for doc phase"
      pattern: "from src.document"
---

<objective>
Build the orchestrator skill that guides users through the full template creation workflow: discover trending nodes, ideate templates from gaps, compose workflow JSON, validate against guidelines, and generate submission docs. The orchestrator carries context between phases and provides suggestions.

Purpose: Individual skills work well in isolation but the real value is the guided end-to-end flow. The orchestrator connects all five skills into a coherent session where each step's output informs the next step's suggestions.

Output: `comfy-flow` Claude Code skill definition and `src/document/orchestrator.py` session state module.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-orchestration/05-CONTEXT.md
@.planning/phases/05-documentation-orchestration/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from all skills the orchestrator chains -->

From .claude/skills/comfy-discover/SKILL.md:
- CLI: `python3 -m src.registry.highlights --mode trending --limit 10`
- CLI: `python3 -m src.registry.search --query "X" --limit 10`
- Modes: trending, new, rising, popular, random

From .claude/skills/comfy-templates/SKILL.md:
- CLI: `python3 -m src.templates.search --query "X"`
- CLI: `python3 -m src.templates.cross_ref --query "node-pack-id" --level pack`
- CLI: `python3 -m src.templates.coverage --mode gaps`

From .claude/skills/comfy-compose/SKILL.md:
- CLI: `python -m src.composer.compose --scaffold <name> --output workflow.json`
- API: WorkflowGraph, scaffold_from_template, save_workflow

From .claude/skills/comfy-validate/SKILL.md:
- CLI: `python -m src.validator.validate --file workflow.json`
- API: run_validation(workflow, mode="strict")

From src/document/ (Plan 01):
- CLI: `python -m src.document.generate --file workflow.json --name my-template`
- API: generate_index_entry, extract_io_spec, generate_notion_markdown, thumbnail_reminder
- Models: IndexEntry, IOSpec, IOItem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session state model and phase transition logic</name>
  <files>src/document/orchestrator.py, tests/test_orchestrator.py</files>
  <read_first>
    - src/document/__init__.py (confirm exports from Plan 01)
    - src/document/metadata.py (generate_index_entry signature for suggestion logic)
    - src/document/models.py (IndexEntry, IOSpec for type references)
    - .claude/skills/comfy-discover/SKILL.md (discover phase commands)
    - .claude/skills/comfy-templates/SKILL.md (ideate phase commands)
    - .claude/skills/comfy-compose/SKILL.md (compose phase commands)
    - .claude/skills/comfy-validate/SKILL.md (validate phase commands -- read to get CLI path)
  </read_first>
  <action>
**src/document/orchestrator.py** -- Session state and phase transitions:

```python
from enum import Enum
from pydantic import BaseModel, Field
```

- `FlowPhase(str, Enum)`: discover, ideate, compose, validate, document, complete. String enum for serialization.
- `FlowSession(BaseModel)`: Tracks accumulated context through the guided flow.
  - phase (FlowPhase, default FlowPhase.discover)
  - discovered_nodes (list[str], default []): Node pack IDs or names found interesting during discovery
  - template_gaps (list[str], default []): Gap areas identified during ideation
  - scaffold_template (str, default ""): Template name used as scaffold base
  - workflow_path (str, default ""): Path to composed workflow JSON
  - workflow_data (dict, default {}): The raw workflow dict (for passing to validation/docs without re-reading file)
  - validation_passed (bool, default False)
  - validation_issues (list[str], default []): Top issues from validation
  - index_entry (dict, default {}): Generated index entry (serialized IndexEntry)
  - notes (list[str], default []): Free-form notes accumulated during session

- `advance_phase(session: FlowSession) -> FlowPhase`: Returns the next logical phase. discover->ideate, ideate->compose, compose->validate, validate->document (only if validation_passed, else stay at validate), document->complete. Does NOT mutate session -- returns the next phase value.

- `suggest_next_actions(session: FlowSession) -> list[str]`: Returns context-aware suggestion strings based on current phase and accumulated state. Rules:
  - Phase discover + no discovered_nodes: ["Run `python3 -m src.registry.highlights --mode trending` to see what's hot", "Try `python3 -m src.registry.highlights --mode random` for inspiration"]
  - Phase discover + has discovered_nodes: ["Check template coverage: `python3 -m src.templates.cross_ref --query {first_node} --level pack`", "Move to ideation to find gaps"]
  - Phase ideate + no template_gaps: ["Run gap analysis: `python3 -m src.templates.coverage --mode gaps`", "Search templates: `python3 -m src.templates.search --query {first_discovered_node}`"]
  - Phase ideate + has template_gaps: ["Ready to compose! Pick a gap area and scaffold or build from scratch", f"Scaffold from closest template: `python -m src.composer.compose --scaffold <name>`"]
  - Phase compose + no workflow_path: ["Compose a workflow using comfy-compose skill", "Or scaffold: `python -m src.composer.compose --scaffold <template> --output workflow.json`"]
  - Phase compose + has workflow_path: [f"Workflow saved to {session.workflow_path}. Run validation next.", "Move to validation to check guideline compliance"]
  - Phase validate + not validation_passed: ["Fix issues and re-validate: `python -m src.validator.validate --file {session.workflow_path}`"] + [f"Issue: {issue}" for issue in session.validation_issues[:3]]
  - Phase validate + validation_passed: ["Validation passed! Generate submission docs next.", f"Run: `python -m src.document.generate --file {session.workflow_path} --name <template-name>`"]
  - Phase document: ["Generate index.json and Notion markdown", "Review thumbnail requirements", "Submit workflow to Comfy Cloud for testing"]
  - Phase complete: ["Session complete! Your template is ready for submission."]
  - Default: ["Continue with the current phase."]

- `format_session_status(session: FlowSession) -> str`: Return multi-line status string showing:
  ```
  ## Template Creation Flow

  **Current Phase:** {phase.value.title()} {phase_emoji}

  ### Progress
  - [x] Discover {if discovered_nodes: f"({len} nodes noted)"}
  - [x/partial/ ] Ideate {if template_gaps: f"({len} gaps found)"}
  ...etc for each phase, checked if past that phase

  ### Context
  {if discovered_nodes: "**Nodes of interest:** " + ", ".join}
  {if scaffold_template: "**Scaffolded from:** " + scaffold_template}
  {if workflow_path: "**Workflow:** " + workflow_path}
  {if validation_passed: "**Validation:** PASSED" else if validation_issues: "**Validation:** FAILED"}

  ### Suggested Next Steps
  {numbered list from suggest_next_actions}
  ```
  Use these phase emojis: discover="(1/5)", ideate="(2/5)", compose="(3/5)", validate="(4/5)", document="(5/5)", complete="(done)".

Update `src/document/__init__.py` to add orchestrator exports:
```python
from src.document.orchestrator import FlowSession, FlowPhase, advance_phase, suggest_next_actions, format_session_status
```
Add these to __all__.

**tests/test_orchestrator.py** -- Tests:

1. `test_flow_phase_order`: FlowPhase enum has 6 values in expected order.
2. `test_advance_discover_to_ideate`: advance_phase(session with phase=discover) returns FlowPhase.ideate.
3. `test_advance_validate_blocks_on_failure`: session with phase=validate, validation_passed=False. advance_phase returns FlowPhase.validate (stays).
4. `test_advance_validate_proceeds_on_pass`: session with phase=validate, validation_passed=True. advance_phase returns FlowPhase.document.
5. `test_advance_complete`: advance_phase(session with phase=complete) returns FlowPhase.complete (terminal).
6. `test_suggest_discover_empty`: suggest_next_actions(fresh session) returns list containing "trending".
7. `test_suggest_discover_with_nodes`: session with discovered_nodes=["impact-pack"]. Suggestions mention "cross_ref" and "impact-pack".
8. `test_suggest_validate_failed`: session with phase=validate, validation_passed=False, validation_issues=["Custom node usage"]. Suggestions mention "Fix" and "Custom node usage".
9. `test_suggest_validate_passed`: session with phase=validate, validation_passed=True, workflow_path="out.json". Suggestions mention "Generate" and "docs".
10. `test_format_session_status`: format_session_status(session with phase=compose, discovered_nodes=["test-node"]) returns string containing "Compose", "test-node", "3/5".
11. `test_session_serialization`: FlowSession can round-trip through model_dump() and FlowSession(**data).
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -m pytest tests/test_orchestrator.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class FlowPhase" src/document/orchestrator.py
    - grep -q "class FlowSession" src/document/orchestrator.py
    - grep -q "def advance_phase" src/document/orchestrator.py
    - grep -q "def suggest_next_actions" src/document/orchestrator.py
    - grep -q "def format_session_status" src/document/orchestrator.py
    - grep -q "FlowSession" src/document/__init__.py
    - grep -q "test_advance_validate_blocks_on_failure" tests/test_orchestrator.py
    - grep -q "test_suggest_discover_with_nodes" tests/test_orchestrator.py
  </acceptance_criteria>
  <done>FlowSession tracks state across all 5 phases. advance_phase enforces validation gate. suggest_next_actions returns context-aware CLI commands. format_session_status shows progress and suggestions. All 11 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator Claude Code skill definition</name>
  <files>.claude/skills/comfy-flow/SKILL.md</files>
  <read_first>
    - src/document/orchestrator.py (just created -- read to confirm actual exports and function signatures)
    - .claude/skills/comfy-discover/SKILL.md (discover phase reference)
    - .claude/skills/comfy-templates/SKILL.md (ideate phase reference)
    - .claude/skills/comfy-compose/SKILL.md (compose phase reference)
    - .claude/skills/comfy-validate/SKILL.md (validate phase reference -- read to get exact CLI command path)
    - .claude/skills/comfy-document/SKILL.md (document phase reference, from Plan 01)
  </read_first>
  <action>
Create `.claude/skills/comfy-flow/SKILL.md` -- the orchestrator skill that ties all skills together.

Frontmatter:
```yaml
---
name: comfy-flow
description: "Guided end-to-end template creation flow: discover trending nodes, ideate from gaps, compose workflows, validate against guidelines, and generate submission docs"
---
```

Content structure:

**# Skill: comfy-flow**

One-line summary: Guided template creation from idea to submission docs.

**## When to Use**
- User wants to create a new template from scratch with guidance
- User says "let's make a template" or "start a new template"
- User wants the full workflow: discover → ideate → compose → validate → document

**## The Five Phases**

Table showing each phase with its skill, purpose, and key output:

| Phase | Skill | What Happens | Key Output |
|-------|-------|-------------|------------|
| 1. Discover | comfy-discover | Browse trending/new nodes, find interesting capabilities | List of promising nodes |
| 2. Ideate | comfy-templates | Check template coverage, find gaps worth filling | Template gap or concept |
| 3. Compose | comfy-compose | Build workflow JSON from scratch or scaffold from template | Valid workflow.json |
| 4. Validate | comfy-validate | Check guideline compliance, flag issues | Validation report (must pass) |
| 5. Document | comfy-document | Generate index.json entry, Notion markdown, thumbnail reminders | Submission-ready docs |

**## How It Works**

The flow maintains a session with context carried between phases. Claude tracks:
- Discovered nodes of interest
- Template gaps identified
- Composed workflow path and data
- Validation status
- Generated documentation

**### Starting the Flow**

```python
from src.document import FlowSession, FlowPhase, suggest_next_actions, format_session_status

session = FlowSession()
print(format_session_status(session))
```

This prints the current phase, progress, and suggested next actions.

**### Phase 1: Discover**
- Use comfy-discover skill commands to browse nodes
- When user finds interesting nodes, note them in session.discovered_nodes
- Suggest: `python3 -m src.registry.highlights --mode trending --limit 10`

**### Phase 2: Ideate**
- Use comfy-templates skill to check coverage and find gaps
- Cross-reference discovered nodes against existing templates
- When user identifies a gap or concept, note in session.template_gaps
- Suggest: `python3 -m src.templates.coverage --mode gaps`

**### Phase 3: Compose**
- Use comfy-compose skill to build the workflow
- If scaffolding from existing template, note session.scaffold_template
- Save to session.workflow_path when done
- Suggest: `python -m src.composer.compose --scaffold <template> --output workflow.json`

**### Phase 4: Validate**
- Run validation on composed workflow
- Must pass before proceeding to docs
- If issues found, fix and re-validate
- Suggest: `python -m src.validator.validate --file workflow.json`

**### Phase 5: Document**
- Generate index.json entry and Notion submission markdown
- Show thumbnail requirements
- Suggest: `python -m src.document.generate --file workflow.json --name my-template`

**## Flexible Entry Points**

Users can enter at any phase:
- "I already have a workflow, validate it" → start at Phase 4
- "I have a validated workflow, generate docs" → start at Phase 5
- "Show me what's trending" → start at Phase 1

To start at a specific phase:
```python
session = FlowSession(phase=FlowPhase.validate, workflow_path="my-workflow.json")
```

**## Context-Aware Suggestions**

After each action, call `suggest_next_actions(session)` to get relevant next steps. The suggestions adapt based on:
- What the user has done so far (discovered nodes, gaps found, etc.)
- Current phase
- Validation status (blocks advancement if failed)

**## Session Status**

At any point, `format_session_status(session)` shows:
- Current phase (1-5)
- Checklist of completed phases
- Accumulated context (nodes, gaps, workflow path)
- Suggested next actions with concrete CLI commands

**## Usage Examples**

```
"Let's create a new template"
"Start the template creation flow"
"I want to make a video template using Wan 2.1"
"I have workflow.json ready, help me validate and document it"
"What's the next step?" (shows session status)
"Generate submission docs for my workflow"
```

**## Key Behaviors**

- Each phase builds on the previous one's output
- Validation must pass before documentation (but user can override with "skip validation")
- Thumbnail reminder is always shown during documentation phase
- Session state is in-memory (not persisted to disk) -- one continuous Claude Code conversation
- User can jump between phases freely; the flow is a guide, not a constraint

**## Key Files**

- `src/document/orchestrator.py` -- FlowSession, FlowPhase, advance_phase, suggest_next_actions, format_session_status
- `.claude/skills/comfy-discover/SKILL.md` -- Phase 1 skill
- `.claude/skills/comfy-templates/SKILL.md` -- Phase 2 skill
- `.claude/skills/comfy-compose/SKILL.md` -- Phase 3 skill
- `.claude/skills/comfy-validate/SKILL.md` -- Phase 4 skill (CLI: `python -m src.validator.validate`)
- `.claude/skills/comfy-document/SKILL.md` -- Phase 5 skill
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && grep -c "comfy-" .claude/skills/comfy-flow/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "comfy-flow" .claude/skills/comfy-flow/SKILL.md
    - grep -q "comfy-discover" .claude/skills/comfy-flow/SKILL.md
    - grep -q "comfy-templates" .claude/skills/comfy-flow/SKILL.md
    - grep -q "comfy-compose" .claude/skills/comfy-flow/SKILL.md
    - grep -q "comfy-validate" .claude/skills/comfy-flow/SKILL.md
    - grep -q "comfy-document" .claude/skills/comfy-flow/SKILL.md
    - grep -q "FlowSession" .claude/skills/comfy-flow/SKILL.md
    - grep -q "suggest_next_actions" .claude/skills/comfy-flow/SKILL.md
    - grep -q "Flexible Entry" .claude/skills/comfy-flow/SKILL.md
  </acceptance_criteria>
  <done>Orchestrator skill documents all 5 phases with concrete CLI commands, flexible entry points, context-aware suggestions via FlowSession, and references all 5 sub-skills.</done>
</task>

</tasks>

<verification>
1. `python -c "from src.document import FlowSession, FlowPhase, advance_phase, suggest_next_actions, format_session_status"` -- imports work
2. `python -m pytest tests/test_orchestrator.py -x -v` -- all tests pass
3. `python -c "from src.document.orchestrator import FlowSession; s = FlowSession(); print(s.phase)"` -- prints "discover"
4. Verify comfy-flow SKILL.md references all 5 sub-skills
5. Verify advance_phase blocks on failed validation
</verification>

<success_criteria>
- FlowSession tracks discovered nodes, template gaps, workflow path, validation status, and index entry across all 5 phases
- advance_phase enforces validation gate (must pass before document phase)
- suggest_next_actions returns CLI commands relevant to current phase and accumulated context
- format_session_status shows progress checklist with concrete next steps
- comfy-flow SKILL.md documents the full guided flow with flexible entry points
- All 11 orchestrator tests pass
- Skill references all 5 sub-skills with correct CLI commands
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-orchestration/05-02-SUMMARY.md`
</output>
