---
phase: 06-testing-distribution
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [TEST-01]

must_haves:
  truths:
    - "Each skill tested interactively with user, issues flagged and fixed on the spot"
    - "Guided walkthrough covers all 6 skills: discover, templates, validate, compose, document, flow"
    - "User confirms each skill behaves as expected or issues are resolved before moving on"
  artifacts: []
  key_links: []
---

<objective>
Guided interactive testing of all 6 skills with the user. Walk through each skill one at a time — user invokes the skill, flags any issues in behavior, and fixes are applied immediately.

Purpose: Skills were built across Phases 1-5 and audited in 06-01. Interactive testing catches UX issues, confusing output, broken flows, and edge cases that automated tests miss. The user's perspective as a template creator is the quality bar.

Output: All 6 skills tested and confirmed working by the user, with any issues fixed inline.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-distribution/06-CONTEXT.md
</context>

<tasks>

<task type="human-verify">
  <name>Task 1: Guided testing of comfy-discover</name>
  <action>
Walk the user through testing /comfy-discover:
1. Show what the skill does and suggest test prompts
2. User invokes the skill, observes output
3. User flags any issues (wrong output, confusing formatting, missing info, errors)
4. Fix issues immediately if flagged
5. User confirms "good" or we iterate

Test scenarios to suggest:
- "What's trending in ComfyUI right now?"
- "Search for video generation nodes"
- "Show me new nodes from this week"
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-discover skill and confirms behavior is acceptable</details>
    <awaiting>User confirmation that discover skill works correctly, or list of issues to fix</awaiting>
  </checkpoint>
</task>

<task type="human-verify">
  <name>Task 2: Guided testing of comfy-templates</name>
  <action>
Walk the user through testing /comfy-templates:
- "What templates exist for video?"
- "What popular nodes don't have templates yet?"
- "Show me the flux templates"

Fix any issues flagged.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-templates skill and confirms behavior</details>
    <awaiting>User confirmation or issues to fix</awaiting>
  </checkpoint>
</task>

<task type="human-verify">
  <name>Task 3: Guided testing of comfy-validate</name>
  <action>
Walk the user through testing /comfy-validate:
- Validate a known-good workflow (should pass)
- Validate a workflow with issues (should flag them)
- Test strict vs lenient mode

Fix any issues flagged.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-validate skill and confirms behavior</details>
    <awaiting>User confirmation or issues to fix</awaiting>
  </checkpoint>
</task>

<task type="human-verify">
  <name>Task 4: Guided testing of comfy-compose</name>
  <action>
Walk the user through testing /comfy-compose:
- Scaffold a workflow from an existing template
- Build a simple workflow from scratch
- Test node swapping or modification

Fix any issues flagged.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-compose skill and confirms behavior</details>
    <awaiting>User confirmation or issues to fix</awaiting>
  </checkpoint>
</task>

<task type="human-verify">
  <name>Task 5: Guided testing of comfy-document</name>
  <action>
Walk the user through testing /comfy-document:
- Generate index.json metadata from a workflow
- Generate Notion markdown
- Check IO extraction and thumbnail reminder

Fix any issues flagged.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-document skill and confirms behavior</details>
    <awaiting>User confirmation or issues to fix</awaiting>
  </checkpoint>
</task>

<task type="human-verify">
  <name>Task 6: Guided testing of comfy-flow (full pipeline)</name>
  <action>
Walk the user through the full /comfy-flow pipeline:
- "Let's create a new template"
- Go through discover → ideate → compose → validate → document
- Test phase transitions, context carrying forward, validation gate

This is the most important test — it exercises the orchestrator end-to-end.

Fix any issues flagged.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>User tests comfy-flow end-to-end pipeline and confirms behavior</details>
    <awaiting>User confirmation or issues to fix</awaiting>
  </checkpoint>
</task>

</tasks>

<verification>
- User has tested all 6 skills interactively
- All flagged issues resolved
- User confirms toolkit behavior meets expectations
</verification>

<success_criteria>
All 6 skills tested by the user with real usage. Issues caught and fixed inline. User confirms the toolkit works as expected for template creation workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-distribution/06-02-SUMMARY.md`
</output>
