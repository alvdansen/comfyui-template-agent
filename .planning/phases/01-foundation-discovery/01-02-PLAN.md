---
phase: 01-foundation-discovery
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/registry/highlights.py
  - src/registry/search.py
  - src/registry/spec.py
  - tests/test_highlights.py
  - tests/test_search.py
  - tests/test_spec.py
  - .claude/skills/comfy-discover/SKILL.md
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05

must_haves:
  truths:
    - "User can query trending nodes and see them ranked by download velocity with recency boost"
    - "User can query new nodes and see recently published nodes with downloads"
    - "User can query rising nodes and see nodes under 90 days old with fast adoption"
    - "User can query popular nodes and see the highest-downloaded nodes"
    - "User can get weighted random suggestions biased toward quality"
    - "User can get truly random suggestions from the full pool"
    - "User can search nodes by name substring and get relevant results"
    - "User can search nodes by input or output type"
    - "User can filter any discovery results by media category (video, image, audio, 3d)"
    - "User can inspect a custom node pack and see all its individual nodes with I/O specs"
    - "All discovery output includes node ID, name, author, description, download count, star count, and repo link"
  artifacts:
    - path: "src/registry/highlights.py"
      provides: "Trending/new/rising/popular/random node discovery"
      exports: ["get_highlights", "format_results"]
      min_lines: 80
    - path: "src/registry/search.py"
      provides: "Node search by name, category, and I/O type"
      exports: ["search_nodes", "search_by_type"]
      min_lines: 40
    - path: "src/registry/spec.py"
      provides: "Node pack inspection with I/O specs"
      exports: ["get_pack_nodes", "format_pack_detail"]
      min_lines: 40
    - path: ".claude/skills/comfy-discover/SKILL.md"
      provides: "Claude Code skill definition for node discovery"
      contains: "comfy-discover"
    - path: "tests/test_highlights.py"
      provides: "Tests for all discovery modes"
      min_lines: 60
    - path: "tests/test_search.py"
      provides: "Tests for search functionality"
      min_lines: 40
    - path: "tests/test_spec.py"
      provides: "Tests for node pack inspection"
      min_lines: 30
  key_links:
    - from: "src/registry/highlights.py"
      to: "src/shared/http.py"
      via: "uses get_client and fetch_json"
      pattern: "from.*shared.*http.*import"
    - from: "src/registry/highlights.py"
      to: "src/shared/cache.py"
      via: "uses DiskCache for highlight caching"
      pattern: "from.*shared.*cache.*import"
    - from: "src/registry/highlights.py"
      to: "src/shared/categories.py"
      via: "uses classify_node for category filtering"
      pattern: "from.*shared.*categories.*import"
    - from: "src/registry/search.py"
      to: "src/shared/http.py"
      via: "uses fetch_json for /nodes?search= and /comfy-nodes"
      pattern: "from.*shared.*http.*import"
    - from: "src/registry/spec.py"
      to: "src/registry/models.py"
      via: "uses ComfyNode model for type-safe node data"
      pattern: "from.*registry.*models.*import"
    - from: ".claude/skills/comfy-discover/SKILL.md"
      to: "src/registry/highlights.py"
      via: "skill instructions reference python3 -m src.registry.highlights"
      pattern: "python3 -m src.registry"
---

<objective>
Implement all five discovery requirements (DISC-01 through DISC-05) as registry modules, create the discovery skill, and add comprehensive tests.

Purpose: This is the user-facing capability of Phase 1 -- template creators can discover nodes from the registry through natural language via Claude Code.
Output: Three registry modules (highlights, search, spec), one Claude Code skill, and passing test suites.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-discovery/01-CONTEXT.md
@.planning/phases/01-foundation-discovery/01-RESEARCH.md
@.planning/phases/01-foundation-discovery/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (shared infrastructure) -->
From src/shared/http.py:
```python
def get_client() -> httpx.Client
def fetch_json(client: httpx.Client, path: str, params: dict | None = None) -> dict
```

From src/shared/cache.py:
```python
class DiskCache:
    def get(self, key: str, ttl: int) -> dict | None
    def set(self, key: str, data: Any) -> None
    def clear(self, key: str | None = None) -> None
```

From src/shared/config.py:
```python
BASE_URL = "https://api.comfy.org"
CACHE_TTLS = {"highlights": 3600, "search": 900, "spec": 86400, "core_nodes": 604800}
CACHE_DIR = Path(...)
```

From src/shared/categories.py:
```python
CATEGORY_KEYWORDS: dict[str, list[str]]
def classify_node(name: str, description: str, category: str = "") -> list[str]
```

From src/registry/models.py:
```python
class NodePack(BaseModel):  # id, name, author, description, downloads, github_stars, rating, ...
class ComfyNode(BaseModel):  # comfy_node_name, category, input_types, return_types, ...
    def parsed_input_types(self) -> dict
    def parsed_return_types(self) -> list
class SearchResult(BaseModel):  # nodes, total, page, limit, totalPages
class ComfyNodeResult(BaseModel):  # comfy_nodes, total
```

<!-- Reference implementation to adapt: -->
From C:/Users/minta/Projects/comfy-tip/highlights.py:
- _score_trending(node) — dl_per_day * (1 + star_bonus) * recency
- _score_rising(node) — dl_per_day * (90 - days) for nodes < 90 days
- get_tip(category) — trending/new/popular/rising/random selection
- fetch_highlights(pages=6) — bulk fetch with caching
- format_tip(pick, label) — human-readable output formatting

<!-- Registry API endpoints (verified): -->
GET /nodes?page=N&limit=50 → paginated node packs (do NOT use sort= params, causes 500)
GET /nodes?search=X → text search on node packs
GET /comfy-nodes?node_id=X&limit=100 → individual nodes for a pack with I/O specs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discovery modules — highlights, search, and spec</name>
  <files>
    src/registry/highlights.py,
    src/registry/search.py,
    src/registry/spec.py
  </files>
  <read_first>
    C:/Users/minta/Projects/comfy-tip/highlights.py
    src/shared/http.py
    src/shared/cache.py
    src/shared/config.py
    src/shared/categories.py
    src/registry/models.py
  </read_first>
  <action>
Create src/registry/highlights.py — adapt from comfy-tip/highlights.py:
- Import and use get_client, fetch_json from src.shared.http
- Import and use DiskCache from src.shared.cache
- Import CACHE_TTLS from src.shared.config
- Import NodePack from src.registry.models
- Import classify_node from src.shared.categories

Functions:
- fetch_all_nodes(pages: int = 6) -> list[NodePack]: Bulk fetch from /nodes, filter to status=="NodeStatusActive", parse through NodePack model, cache with DiskCache using key "highlights" and TTL from CACHE_TTLS["highlights"]. Return list of NodePack.
- _score_trending(node: NodePack) -> float: Adapt from comfy-tip. Uses days_since(node.created_at), dl_per_day = node.downloads / days, star_bonus = math.log(node.github_stars + 1, 2), recency = max(1, 10 - (days / 30)). Return dl_per_day * (1 + star_bonus) * recency.
- _score_rising(node: NodePack) -> float: Adapt from comfy-tip. Return 0 if days > 90, else dl_per_day * (90 - days).
- _days_since(date_str: str) -> int: Same as comfy-tip. Parse ISO date, return days since, default 365.
- get_highlights(mode: str = "trending", limit: int = 10, category: str | None = None, truly_random: bool = False) -> list[NodePack]:
  - Fetch all nodes via fetch_all_nodes()
  - If category is set, filter via classify_node(n.name, n.description) keeping only nodes matching category
  - Mode "trending": sort by _score_trending, return top `limit`
  - Mode "new": filter to last 14 days with downloads > 0, sort by created_at desc, return top `limit`. Fallback to most recent if none qualify.
  - Mode "popular": sort by downloads desc, return top `limit`
  - Mode "rising": sort by _score_rising, filter to score > 0, return top `limit`
  - Mode "random": if truly_random, random.sample from full pool. If weighted (default), build weight list where weight = downloads + github_stars + 1, use random.choices with weights, return `limit` unique picks.
  - Raise ValueError for unknown mode
- format_results(nodes: list[NodePack], mode: str) -> str: Format as table/cards: "Name | Author | Downloads | Stars | Description (truncated 100 chars) | Repo link". Include mode label header.

Add __main__ block with argparse:
- --mode: trending|new|rising|popular|random (default: trending)
- --limit: int (default: 10)
- --category: video|image|audio|3d (optional)
- --truly-random: flag for random mode
- --json: output raw JSON instead of formatted text

Create src/registry/search.py:
- Import get_client, fetch_json, DiskCache, CACHE_TTLS, NodePack, SearchResult, classify_node

Functions:
- search_nodes(query: str, category: str | None = None, limit: int = 20) -> list[NodePack]:
  - Use fetch_json with path="/nodes", params={"search": query, "limit": limit}
  - Parse through SearchResult model
  - If category set, filter results via classify_node
  - Cache with key f"search_{query}_{category}" and TTL CACHE_TTLS["search"]
  - Return list of NodePack
- search_by_type(input_type: str | None = None, output_type: str | None = None, limit: int = 20) -> list[dict]:
  - This requires fetching comfy_nodes and matching input_types/return_types
  - Use fetch_json with path="/comfy-nodes" (Note: this endpoint needs a node_id, so this function should search across known packs)
  - Alternative approach: search_nodes with type name as query, then for each result, fetch comfy_nodes and filter by parsed I/O types
  - Return list of dicts with {pack: NodePack, matching_nodes: list[ComfyNode]}
- format_search_results(nodes: list[NodePack], query: str) -> str: Format as table similar to highlights

Add __main__ block with argparse:
- --query: search text (required)
- --category: filter category (optional)
- --type-input: filter by input type (optional)
- --type-output: filter by output type (optional)
- --limit: int (default: 20)
- --json: output raw JSON

Create src/registry/spec.py:
- Import get_client, fetch_json, DiskCache, CACHE_TTLS, ComfyNode, ComfyNodeResult

Functions:
- get_pack_nodes(node_pack_id: str, limit: int = 100) -> list[ComfyNode]:
  - Fetch from /comfy-nodes?node_id={node_pack_id}&limit={limit}
  - Parse through ComfyNodeResult model
  - Handle pagination: if total > limit, fetch additional pages
  - Cache with key f"spec_{node_pack_id}" and TTL CACHE_TTLS["spec"]
  - Return list of ComfyNode
- format_pack_detail(pack_id: str, nodes: list[ComfyNode], summary: bool = True) -> str:
  - Summary mode: show node count, list node names with categories
  - Detail mode: show full I/O specs for each node (parsed_input_types, parsed_return_types)
  - Group by category if many nodes

Add __main__ block with argparse:
- node_pack_id: positional arg
- --limit: int (default: 100)
- --detail: flag for full I/O specs
- --json: output raw JSON
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -c "from src.registry.highlights import get_highlights, fetch_all_nodes, format_results; from src.registry.search import search_nodes, format_search_results; from src.registry.spec import get_pack_nodes, format_pack_detail; print('All registry imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/registry/highlights.py contains `def get_highlights(` with mode, limit, category, truly_random params
    - src/registry/highlights.py contains `def _score_trending(` and `def _score_rising(`
    - src/registry/highlights.py contains `def fetch_all_nodes(` using DiskCache
    - src/registry/highlights.py contains `if __name__` block with argparse
    - src/registry/search.py contains `def search_nodes(` with query, category, limit params
    - src/registry/search.py contains `def search_by_type(` with input_type, output_type params
    - src/registry/spec.py contains `def get_pack_nodes(` and `def format_pack_detail(`
    - All three modules import from src.shared (http, cache, config, categories) and src.registry.models
    - `python -c "from src.registry.highlights import get_highlights"` exits 0
  </acceptance_criteria>
  <done>All three registry modules implement DISC-01 through DISC-05, use shared infrastructure, support CLI invocation with argparse, and output formatted results</done>
</task>

<task type="auto">
  <name>Task 2: Tests for discovery modules</name>
  <files>
    tests/test_highlights.py,
    tests/test_search.py,
    tests/test_spec.py
  </files>
  <read_first>
    src/registry/highlights.py
    src/registry/search.py
    src/registry/spec.py
    tests/conftest.py
  </read_first>
  <action>
Create tests/test_highlights.py:
- Use conftest fixtures for sample data, mock httpx responses
- test_fetch_all_nodes_returns_node_packs: mock /nodes response, verify returns list[NodePack], verify status filter works
- test_fetch_all_nodes_caches: call twice, verify second call uses cache (mock only called once)
- test_score_trending_higher_for_newer_popular: create two nodes, newer one with same downloads should score higher
- test_score_trending_higher_for_more_stars: same age/downloads, more stars = higher score
- test_score_rising_zero_for_old_nodes: node older than 90 days scores 0
- test_score_rising_positive_for_new_nodes: node under 90 days with downloads scores > 0
- test_get_highlights_trending: mock data, verify returns sorted by trending score desc
- test_get_highlights_new: mock data with mix of old and new, verify only recent nodes returned
- test_get_highlights_popular: verify sorted by downloads desc
- test_get_highlights_rising: verify only nodes < 90 days returned, sorted by rising score
- test_get_highlights_random_weighted: verify weighted random returns nodes (statistical check: high-download nodes appear more often over 100 runs)
- test_get_highlights_random_truly: verify truly_random=True returns any node from pool
- test_get_highlights_unknown_mode: verify ValueError raised for mode="invalid"
- test_get_highlights_with_category_filter: mock nodes with different categories, filter to "video", verify only video nodes returned
- test_format_results_includes_fields: verify output string contains name, author, downloads

Create tests/test_search.py:
- test_search_nodes_basic: mock /nodes?search=X response, verify returns list[NodePack]
- test_search_nodes_caches: verify cache hit on second call
- test_search_nodes_with_category: mock response, filter to "audio", verify filtered
- test_search_nodes_empty_results: mock empty response, verify returns []
- test_format_search_results: verify output contains query and node names

Create tests/test_spec.py:
- test_get_pack_nodes_basic: mock /comfy-nodes response, verify returns list[ComfyNode]
- test_get_pack_nodes_caches: verify cache hit
- test_get_pack_nodes_pagination: mock response with total > limit, verify fetches multiple pages
- test_format_pack_detail_summary: verify summary mode shows node count and names
- test_format_pack_detail_full: verify detail mode shows I/O types
- test_comfy_node_io_parsing: verify parsed_input_types returns dict from JSON string

All tests must mock HTTP calls (no real API calls in tests). Use unittest.mock.patch or pytest-monkeypatch on fetch_json or httpx.Client.

Run: python -m pytest tests/test_highlights.py tests/test_search.py tests/test_spec.py -v --tb=short
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -m pytest tests/test_highlights.py tests/test_search.py tests/test_spec.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_highlights.py contains at least 12 test functions
    - tests/test_highlights.py contains test_get_highlights_trending, test_get_highlights_random_weighted, test_get_highlights_with_category_filter
    - tests/test_search.py contains at least 4 test functions
    - tests/test_spec.py contains at least 4 test functions
    - No test makes real HTTP calls (all mock fetch_json or httpx.Client)
    - `python -m pytest tests/test_highlights.py tests/test_search.py tests/test_spec.py -v` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Full test suite passes for all discovery modules, covering scoring heuristics, mode selection, category filtering, search, pagination, caching, and output formatting</done>
</task>

<task type="auto">
  <name>Task 3: Claude Code skill definition for node discovery</name>
  <files>
    .claude/skills/comfy-discover/SKILL.md
  </files>
  <read_first>
    .planning/phases/01-foundation-discovery/01-CONTEXT.md
    src/registry/highlights.py
    src/registry/search.py
    src/registry/spec.py
  </read_first>
  <action>
Create .claude/skills/comfy-discover/SKILL.md:

Frontmatter:
- name: comfy-discover
- description: "Discover trending, new, and interesting ComfyUI nodes from the registry for template inspiration"

Content structure:

# Node Discovery

## What This Does
Surfaces interesting nodes from the ComfyUI registry for template creation inspiration. Browse trending, new, rising, and popular nodes, search by name or type, filter by media category, and inspect node packs.

## Quick Start
Just describe what you're looking for in natural language:
- "What's trending in ComfyUI?"
- "Show me new video processing nodes"
- "Find nodes for audio generation"
- "Surprise me with something random"
- "What nodes does ComfyUI-Impact-Pack include?"
- "Find nodes that output MASK type"

## How It Works

### Browse Discovery Modes
```bash
python3 -m src.registry.highlights --mode trending --limit 10
python3 -m src.registry.highlights --mode new --limit 10
python3 -m src.registry.highlights --mode rising --limit 10
python3 -m src.registry.highlights --mode popular --limit 10
python3 -m src.registry.highlights --mode random --limit 5
python3 -m src.registry.highlights --mode random --truly-random --limit 5
```

### Filter by Media Category
Add --category to any browse command:
```bash
python3 -m src.registry.highlights --mode trending --category video
python3 -m src.registry.highlights --mode popular --category audio
```
Categories: video, image, audio, 3d

### Search Nodes
```bash
python3 -m src.registry.search --query "upscale" --limit 10
python3 -m src.registry.search --query "controlnet" --category image
python3 -m src.registry.search --type-input IMAGE --type-output MASK
```

### Inspect Node Pack
```bash
python3 -m src.registry.spec <node-pack-id>
python3 -m src.registry.spec comfyui-impact-pack --detail
```

### JSON Output
Add --json to any command for raw JSON output (useful for piping to other tools).

## Output Format
Results show as summary cards by default:
- Name, Author, Downloads, Stars, Description, Repository link
- Node ID is always included for cross-referencing with other skills

When user asks for details on a specific node or pack, show full I/O specifications.

## Tips
- Discovery results are cached (1hr for browsing, 15min for search, 24hr for specs)
- Use "random" mode for creative inspiration; "trending" for popular choices
- Node IDs from results can be used with other comfy-* skills
- The category filter uses keyword matching, not registry tags (which are empty)
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && test -f .claude/skills/comfy-discover/SKILL.md && grep -c "python3 -m src.registry" .claude/skills/comfy-discover/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/comfy-discover/SKILL.md exists
    - SKILL.md frontmatter contains `name: comfy-discover`
    - SKILL.md contains "python3 -m src.registry.highlights" command examples
    - SKILL.md contains "python3 -m src.registry.search" command examples
    - SKILL.md contains "python3 -m src.registry.spec" command examples
    - SKILL.md contains natural language prompt examples ("What's trending", "Show me new")
    - SKILL.md documents --category, --json, --detail flags
    - grep for "python3 -m src.registry" returns at least 6 matches
  </acceptance_criteria>
  <done>Claude Code skill file exists with natural language examples, all CLI commands documented, and output format described</done>
</task>

</tasks>

<verification>
Full Phase 1 verification after both plans complete:

1. Browse modes work:
   `python3 -m src.registry.highlights --mode trending --limit 5`
   `python3 -m src.registry.highlights --mode random --limit 3`

2. Category filtering works:
   `python3 -m src.registry.highlights --mode trending --category video --limit 5`

3. Search works:
   `python3 -m src.registry.search --query "upscale" --limit 5`

4. Pack inspection works:
   `python3 -m src.registry.spec comfyui-impact-pack`

5. All tests pass:
   `python -m pytest tests/ -v --tb=short`

6. Skill file exists:
   `cat .claude/skills/comfy-discover/SKILL.md`
</verification>

<success_criteria>
- DISC-01: All 5 browse modes return ranked results with download counts and descriptions
- DISC-02: Search by name returns relevant results, search by I/O type filters correctly
- DISC-03: Category filter on any browse/search command restricts to video/image/audio/3d
- DISC-04: Pack inspection shows all individual nodes with I/O specifications
- DISC-05: Random mode offers weighted (default) and truly random (--truly-random flag)
- All tests pass (25+ tests across 3 test files)
- Skill file enables natural language discovery via Claude Code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-02-SUMMARY.md`
</output>
