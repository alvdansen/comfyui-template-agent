---
phase: 01-foundation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/shared/__init__.py
  - src/shared/http.py
  - src/shared/cache.py
  - src/shared/config.py
  - src/shared/format_detector.py
  - src/shared/categories.py
  - src/registry/__init__.py
  - src/registry/models.py
  - data/core_nodes.json
  - data/guidelines.json
  - data/guidelines/image.png
  - data/guidelines/image_2.png
  - data/guidelines/image_3.png
  - data/guidelines/image_4.png
  - data/guidelines/image_7.png
  - data/guidelines/image_8.png
  - data/guidelines/image_10.png
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_shared.py
autonomous: true
requirements:
  - DISC-03

must_haves:
  truths:
    - "HTTP client can make GET requests to api.comfy.org with configurable timeout and retry"
    - "Cache stores and retrieves JSON data with per-query-type TTL"
    - "Format detector distinguishes workflow format from API format"
    - "Category classifier maps node text to media categories (image, video, audio, 3d, utility)"
    - "Pydantic models parse registry API responses without errors"
    - "Core node whitelist exists as a JSON file with 100+ node names"
    - "Guidelines JSON contains structured rules extracted from Notion export"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project config with dependencies and pytest settings"
      contains: "httpx"
    - path: "src/shared/http.py"
      provides: "Shared httpx client wrapper"
      exports: ["get_client", "fetch_json"]
    - path: "src/shared/cache.py"
      provides: "Disk-based JSON cache with TTL"
      exports: ["DiskCache"]
    - path: "src/shared/config.py"
      provides: "API URLs, cache paths, TTL config"
      contains: "BASE_URL"
    - path: "src/shared/format_detector.py"
      provides: "Workflow vs API format detection"
      exports: ["detect_format"]
    - path: "src/shared/categories.py"
      provides: "Media category keyword mapping"
      exports: ["classify_node", "CATEGORY_KEYWORDS"]
    - path: "src/registry/models.py"
      provides: "Pydantic models for NodePack, ComfyNode, SearchResult"
      exports: ["NodePack", "ComfyNode", "SearchResult"]
    - path: "data/core_nodes.json"
      provides: "Core ComfyUI node whitelist"
      min_lines: 10
    - path: "data/guidelines.json"
      provides: "Structured template creation guidelines"
      min_lines: 20
  key_links:
    - from: "src/shared/http.py"
      to: "src/shared/config.py"
      via: "imports BASE_URL, CLIENT_TIMEOUT"
      pattern: "from.*config.*import"
    - from: "src/shared/cache.py"
      to: "src/shared/config.py"
      via: "imports CACHE_DIR, CACHE_TTLS"
      pattern: "from.*config.*import"
    - from: "src/registry/models.py"
      to: "pydantic"
      via: "BaseModel inheritance"
      pattern: "class.*BaseModel"
---

<objective>
Project scaffolding, shared infrastructure, data models, and data extraction for Phase 1.

Purpose: Every discovery module (Plan 02) depends on these shared components. HTTP client, cache, config, Pydantic models, format detector, category mapping, and static data files must exist first.
Output: Working project skeleton with all shared utilities, data files, and passing unit tests.
</objective>

<execution_context>
@C:/Users/minta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-discovery/01-CONTEXT.md
@.planning/phases/01-foundation-discovery/01-RESEARCH.md

<interfaces>
<!-- Reference implementation to adapt (NOT copy verbatim): -->
From C:/Users/minta/Projects/comfy-tip/highlights.py:
- _fetch_json(url) — urllib-based, upgrade to httpx
- _load_cache() / _save_cache(nodes) — JSON file cache with TTL
- API_BASE = "https://api.comfy.org"
- CACHE_TTL = 3600

<!-- Registry API response shapes (verified 2026-03-18): -->
GET /nodes → { nodes: [...], total: int, page: int, limit: int, totalPages: int }
GET /comfy-nodes?node_id=X → { comfy_nodes: [...], total: int }
Node fields: id, name, author, description, downloads, github_stars, rating, created_at, repository, tags, icon, status, publisher
ComfyNode fields: comfy_node_name, category, input_types (JSON string), return_types (JSON string), return_names, deprecated, experimental
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding and shared infrastructure</name>
  <files>
    pyproject.toml,
    src/__init__.py,
    src/shared/__init__.py,
    src/shared/config.py,
    src/shared/http.py,
    src/shared/cache.py,
    src/shared/format_detector.py,
    src/shared/categories.py,
    src/registry/__init__.py,
    src/registry/models.py
  </files>
  <read_first>
    C:/Users/minta/Projects/comfy-tip/highlights.py
    .planning/phases/01-foundation-discovery/01-RESEARCH.md
  </read_first>
  <action>
Create pyproject.toml with:
- name: "comfyui-template-agent"
- python: ">=3.12"
- dependencies: ["httpx>=0.28", "pydantic>=2.0"]
- optional dev dependencies: ["pytest>=9.0", "ruff>=0.15"]
- [tool.pytest.ini_options]: testpaths = ["tests"], python_files = "test_*.py"
- [tool.ruff]: line-length = 100, target-version = "py312"

Create src/shared/config.py with:
- BASE_URL = "https://api.comfy.org"
- CLIENT_TIMEOUT = httpx.Timeout(15.0, connect=5.0)
- USER_AGENT = "ComfyTemplateAgent/1.0"
- CACHE_DIR = Path(__file__).parent.parent.parent / "data" / "cache"
- CACHE_TTLS dict: {"highlights": 3600, "search": 900, "spec": 86400, "core_nodes": 604800}
- GITHUB_TOKEN env var support: os.environ.get("GITHUB_TOKEN", "")

Create src/shared/http.py with:
- get_client() -> httpx.Client: returns httpx.Client(base_url=BASE_URL, timeout=CLIENT_TIMEOUT, headers={"User-Agent": USER_AGENT})
- fetch_json(client: httpx.Client, path: str, params: dict | None = None) -> dict: client.get(path, params=params), raise_for_status(), return .json()

Create src/shared/cache.py with:
- class DiskCache:
  - __init__(self, cache_dir: Path = CACHE_DIR): creates cache_dir if needed
  - _cache_path(self, key: str) -> Path: returns cache_dir / f"{key}.json"
  - get(self, key: str, ttl: int) -> dict | None: reads JSON, checks timestamp against ttl, returns data or None
  - set(self, key: str, data: Any) -> None: writes {"fetched_at": time.time(), "data": data} as JSON
  - clear(self, key: str | None = None): removes specific key or all cache files

Create src/shared/format_detector.py with:
- detect_format(data: dict) -> str: returns "workflow" if data has "nodes" list, "api" if any value dict has "class_type", else "unknown"

Create src/shared/categories.py with:
- CATEGORY_KEYWORDS dict with keys "video", "image", "audio", "3d" mapping to keyword lists (exact lists from RESEARCH.md code example)
- classify_node(name: str, description: str, category: str = "") -> list[str]: concatenates inputs lowercase, checks keywords, returns matches or ["utility"]

Create src/registry/models.py with Pydantic models:
- class NodePack(BaseModel): id: str, name: str, author: str = "", description: str = "", downloads: int = 0, github_stars: int = Field(0, alias="github_stars"), rating: float = 0, created_at: str = "", repository: str = "", tags: list[str] = [], status: str = "", icon: str = "", model_config = ConfigDict(populate_by_name=True)
- class ComfyNode(BaseModel): comfy_node_name: str, category: str = "", input_types: str = "", return_types: str = "", return_names: str = "", deprecated: bool = False, experimental: bool = False. Add a method parsed_input_types() -> dict that does json.loads(self.input_types) with fallback to {}. Same for parsed_return_types() -> list.
- class SearchResult(BaseModel): nodes: list[NodePack], total: int, page: int, limit: int, totalPages: int = Field(0, alias="totalPages"), model_config = ConfigDict(populate_by_name=True)
- class ComfyNodeResult(BaseModel): comfy_nodes: list[ComfyNode], total: int

Create __init__.py files for src/, src/shared/, src/registry/ (empty or with __all__ exports).
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -c "from src.shared.config import BASE_URL, CACHE_TTLS; from src.shared.http import get_client; from src.shared.cache import DiskCache; from src.shared.format_detector import detect_format; from src.shared.categories import classify_node, CATEGORY_KEYWORDS; from src.registry.models import NodePack, ComfyNode, SearchResult, ComfyNodeResult; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "comfyui-template-agent"` and `"httpx>=0.28"` and `"pydantic>=2.0"` and `testpaths = ["tests"]`
    - src/shared/config.py contains `BASE_URL = "https://api.comfy.org"` and `CACHE_TTLS` and `GITHUB_TOKEN`
    - src/shared/http.py contains `def get_client()` and `def fetch_json(`
    - src/shared/cache.py contains `class DiskCache` with methods `get`, `set`, `clear`
    - src/shared/format_detector.py contains `def detect_format(data: dict) -> str`
    - src/shared/categories.py contains `CATEGORY_KEYWORDS` and `def classify_node(`
    - src/registry/models.py contains `class NodePack(BaseModel)` and `class ComfyNode(BaseModel)` and `class SearchResult(BaseModel)` and `class ComfyNodeResult(BaseModel)`
    - `python -c "from src.shared.config import BASE_URL"` exits 0
  </acceptance_criteria>
  <done>All shared infrastructure modules importable, pyproject.toml configured, Pydantic models parse sample data</done>
</task>

<task type="auto">
  <name>Task 2: Data extraction — core nodes whitelist and guidelines JSON</name>
  <files>
    scripts/extract_core_nodes.py,
    scripts/extract_guidelines.py,
    data/core_nodes.json,
    data/guidelines.json,
    data/guidelines/image.png,
    data/guidelines/image_2.png,
    data/guidelines/image_3.png,
    data/guidelines/image_4.png,
    data/guidelines/image_7.png,
    data/guidelines/image_8.png,
    data/guidelines/image_10.png
  </files>
  <read_first>
    /tmp/notion-export/ComfyUI Template Creation Guidelines 3036d73d36508065828bd336f51ba3eb.md
    .planning/phases/01-foundation-discovery/01-RESEARCH.md
  </read_first>
  <action>
Create scripts/extract_core_nodes.py:
- Uses httpx to fetch ComfyUI GitHub source files
- Fetches https://raw.githubusercontent.com/comfyanonymous/ComfyUI/master/nodes.py
- Extracts node names from NODE_CLASS_MAPPINGS via regex: re.findall(r'"([^"]+)"', mapping_block)
- Fetches https://api.github.com/repos/comfyanonymous/ComfyUI/contents/comfy_extras (with GITHUB_TOKEN header if available)
- For each nodes_*.py file, fetches download_url and extracts NODE_CLASS_MAPPINGS entries
- Outputs sorted unique list to data/core_nodes.json as {"nodes": [...], "extracted_at": "ISO date", "source": "github.com/comfyanonymous/ComfyUI", "count": N}
- Handle GitHub rate limits: if GITHUB_TOKEN env var set, use Authorization header. Print progress to stderr.
- Run the script to generate data/core_nodes.json (should produce 100+ node names)

Create scripts/extract_guidelines.py:
- Reads /tmp/notion-export/ComfyUI Template Creation Guidelines*.md
- Parses markdown into structured JSON with sections:
  - rules: array of {id: str, title: str, description: str, severity: "required"|"recommended", images: [str]}
  - Key rules to extract:
    - "core_node_preference": Use core nodes when possible, avoid custom nodes (image.png)
    - "no_set_get_nodes": Never use Set/Get nodes
    - "subgraph_rules": Settings outside subgraphs, no Preview/Save inside subgraphs (image_7.png, image_8.png)
    - "unique_subgraph_names": No identically-named subgraphs with different internals (image_4.png)
    - "note_color_black": Note nodes must be black background
    - "api_node_color_yellow": API nodes colored yellow
    - "api_badge_position": API provider badge not in top-left (image_3.png)
    - "thumbnail_specs": Requirements for thumbnails (image_2.png)
    - "naming_conventions": Template naming rules
  - submission_process: array of steps
  - thumbnail_requirements: {dimensions: str, format: str, content_types: [str]}
  - color_conventions: {notes: "black", api_nodes: "yellow"}
- Output to data/guidelines.json

Copy the 7 key images from /tmp/notion-export/ to data/guidelines/:
- image.png -> data/guidelines/image.png
- "image 2.png" -> data/guidelines/image_2.png
- "image 3.png" -> data/guidelines/image_3.png
- "image 4.png" -> data/guidelines/image_4.png
- "image 7.png" -> data/guidelines/image_7.png
- "image 8.png" -> data/guidelines/image_8.png
- "image 10.png" -> data/guidelines/image_10.png

Run both extraction scripts to generate the data files.
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -c "import json; d=json.load(open('data/core_nodes.json')); assert len(d['nodes']) > 50, f'Only {len(d[\"nodes\"])} nodes'; print(f'Core nodes: {len(d[\"nodes\"])}')" && python -c "import json; g=json.load(open('data/guidelines.json')); assert 'rules' in g; print(f'Guidelines rules: {len(g[\"rules\"])}')" && ls data/guidelines/image.png data/guidelines/image_2.png data/guidelines/image_4.png</automated>
  </verify>
  <acceptance_criteria>
    - data/core_nodes.json exists and contains a "nodes" array with more than 50 entries
    - data/core_nodes.json contains "extracted_at" and "source" fields
    - data/guidelines.json exists and contains a "rules" array
    - data/guidelines.json contains rules with ids "core_node_preference", "no_set_get_nodes", "subgraph_rules"
    - data/guidelines/ directory contains image.png, image_2.png, image_3.png, image_4.png, image_7.png, image_8.png, image_10.png
    - scripts/extract_core_nodes.py is executable and re-runnable
  </acceptance_criteria>
  <done>Core node whitelist has 100+ nodes extracted from ComfyUI source, guidelines JSON has structured rules with image references, 7 key images copied to data/guidelines/</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for shared infrastructure</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/test_shared.py
  </files>
  <read_first>
    src/shared/http.py
    src/shared/cache.py
    src/shared/config.py
    src/shared/format_detector.py
    src/shared/categories.py
    src/registry/models.py
  </read_first>
  <action>
Create tests/conftest.py with shared fixtures:
- sample_node_pack_data: dict matching registry API /nodes response shape (include id, name, author, description, downloads, github_stars, rating, created_at, repository, tags, status fields with realistic values)
- sample_comfy_node_data: dict matching /comfy-nodes response shape (comfy_node_name, category, input_types as JSON string e.g. '{"required": {"image": ["IMAGE"]}}', return_types as JSON string e.g. '["IMAGE"]')
- sample_search_result: dict matching full /nodes paginated response
- sample_workflow_json: dict with "nodes" list and "links" list (workflow format)
- sample_api_json: dict with numbered keys containing "class_type" (API format)
- tmp_cache_dir: pytest tmp_path fixture for isolated cache testing

Create tests/test_shared.py with test functions:

Test HTTP (mock httpx):
- test_get_client_returns_httpx_client: verify returns httpx.Client with correct base_url and timeout
- test_fetch_json_returns_parsed_json: mock client.get response, verify dict returned
- test_fetch_json_raises_on_http_error: mock 500 response, verify httpx.HTTPStatusError raised

Test Cache:
- test_cache_set_and_get: set data, get within TTL, verify returns data
- test_cache_expired: set data, get with TTL=0, verify returns None
- test_cache_miss: get nonexistent key, verify returns None
- test_cache_clear_specific: set 2 keys, clear one, verify other remains
- test_cache_clear_all: set 2 keys, clear all, verify both gone

Test Format Detector:
- test_detect_workflow_format: pass sample_workflow_json, verify returns "workflow"
- test_detect_api_format: pass sample_api_json, verify returns "api"
- test_detect_unknown_format: pass empty dict, verify returns "unknown"

Test Categories:
- test_classify_video_node: classify_node("AnimateDiff", "animation tool") returns ["video"] (or list containing "video")
- test_classify_image_node: classify_node("KSampler", "image denoising") returns list containing "image"
- test_classify_audio_node: classify_node("AudioGen", "audio generation") returns list containing "audio"
- test_classify_3d_node: classify_node("Mesh3D", "3d mesh processing") returns list containing "3d"
- test_classify_utility_fallback: classify_node("SomeUtil", "does stuff") returns ["utility"]
- test_classify_multi_category: verify a node matching both video and image keywords returns both

Test Pydantic Models:
- test_node_pack_from_api_data: construct NodePack from sample_node_pack_data, verify all fields
- test_comfy_node_parsed_input_types: construct ComfyNode with JSON string input_types, verify parsed_input_types() returns dict
- test_comfy_node_parsed_return_types: construct ComfyNode with JSON string return_types, verify parsed_return_types() returns list
- test_search_result_from_api_data: construct SearchResult from sample_search_result, verify nodes list and total

Run: python -m pytest tests/test_shared.py -v
  </action>
  <verify>
    <automated>cd C:/Users/minta/Projects/comfyui-template-agent && python -m pytest tests/test_shared.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains fixtures: sample_node_pack_data, sample_comfy_node_data, sample_workflow_json, sample_api_json, tmp_cache_dir
    - tests/test_shared.py contains at least 15 test functions
    - tests/test_shared.py contains test_detect_workflow_format, test_detect_api_format, test_classify_video_node, test_cache_set_and_get, test_node_pack_from_api_data
    - `python -m pytest tests/test_shared.py -v` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All shared infrastructure tests pass, covering HTTP client, cache, format detector, category classifier, and Pydantic models</done>
</task>

</tasks>

<verification>
All shared infrastructure importable and tested:
- `python -c "from src.shared.http import get_client; from src.shared.cache import DiskCache; from src.shared.format_detector import detect_format; from src.shared.categories import classify_node; from src.registry.models import NodePack"` exits 0
- `python -m pytest tests/test_shared.py -v` all green
- data/core_nodes.json has 100+ nodes
- data/guidelines.json has structured rules
- data/guidelines/ has 7 images
</verification>

<success_criteria>
- Project skeleton exists with pyproject.toml, src/shared/, src/registry/, tests/, data/, scripts/
- All shared modules importable without errors
- All unit tests pass (15+ tests)
- Core nodes whitelist populated from ComfyUI source
- Guidelines extracted from Notion export to structured JSON
- Key guideline images preserved in data/guidelines/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-01-SUMMARY.md`
</output>
